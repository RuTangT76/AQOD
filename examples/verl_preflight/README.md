# veRL integration probes

These scripts exercise model loading and small synthetic GPU updates on the original two-GPU server. They use machine-specific model paths and are retained as integration examples, not as training recipes or research results. Review their constants and set up the relevant veRL, PyTorch, Transformers, and model dependencies before running them elsewhere.
